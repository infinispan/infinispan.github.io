---
layout: project
title: Blog
---

.row
  .col-md-9
    - for post in page.posts
      .post
      %h3
        = post.date.strftime( '%A, %d %B %Y' )
      %h1
        %a{:href=>post.url}= post.title
      .content
        = post.content
      %em Posted by #{post.author} on #{post.date.strftime( '%Y-%m-%d' )}
      %br
      %em Tags: 
      - post.tags.each do |tag|
        %a{:href=>tag.primary_page.url}
          = tag
  .col-md-3  
    .panel.panel-info
      .panel-heading
        %h3 News
    .panel-body
      = partial ( 'latest-posts.html.haml' )
    .panel.panel-info
      .panel-heading
        %h3 Tags
    .panel-body
      = site.posts_tag_cloud
  %hr
  = page.posts.links
